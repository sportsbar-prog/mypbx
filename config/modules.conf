; ============================================
; Asterisk Modules Configuration
; /etc/asterisk/modules.conf
; ============================================

[modules]
autoload=yes

; ============================================
; Ensure Critical Modules Load
; ============================================

; PJSIP Stack
load = res_pjsip.so
load = res_pjsip_session.so
load = res_pjsip_registrar.so
load = res_pjsip_outbound_registration.so
load = res_pjsip_endpoint_identifier_ip.so
load = res_pjsip_endpoint_identifier_user.so
load = res_pjsip_authenticator_digest.so
load = res_pjsip_transport_websocket.so

; ARI & HTTP
load = res_ari.so
load = res_ari_applications.so
load = res_ari_asterisk.so
load = res_ari_bridges.so
load = res_ari_channels.so
load = res_ari_device_states.so
load = res_ari_endpoints.so
load = res_ari_events.so
load = res_ari_playbacks.so
load = res_ari_recordings.so
load = res_ari_sounds.so
load = res_http_websocket.so

; Codecs
load = codec_ulaw.so
load = codec_alaw.so
load = codec_gsm.so
load = codec_g722.so
load = codec_opus.so

; Applications
load = app_dial.so
load = app_playback.so
load = app_queue.so
load = app_voicemail.so
load = app_directory.so
load = app_confbridge.so
load = app_amd.so

; Resources
load = res_musiconhold.so
load = res_timing_timerfd.so
load = res_rtp_asterisk.so

; ============================================
; Disable Unnecessary Modules (Optional)
; ============================================

; Disable chan_sip (using PJSIP instead)
noload = chan_sip.so

; Disable DAHDI if not using analog/digital cards
; noload = chan_dahdi.so

; Disable console channel
noload = chan_console.so
